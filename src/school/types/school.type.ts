import { BaseType } from '../../common/types/base.type';

export type SchoolType = BaseType & {
  
  school_name: string;
  school_state: string;
  school_city:string;
  school_contact: number;
  
}

